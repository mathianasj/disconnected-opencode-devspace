# Opencode Configuration

This devspace is configured to use the internal AI model at:
`http://redhataiqwen3-coder-next-nvfp4-predictor.coder.svc.cluster.local:8080/v1`

## Setup

The devspace uses a startup script that automatically installs opencode on first run if not already present.

## Usage

1. Start the devspace
2. The startup script will install opencode if needed
3. Run `opencode` commands as needed
